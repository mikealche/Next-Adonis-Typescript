{
  "name": "@template/shared",
  "version": "1.0.0",
  "main": "dist/index.js",
  "private": true,
  "dependencies": {
    "@template/backend": "1.0.0",
    "axios": "^0.21.1",
    "typescript": "^4.1.3"
  },
  "scripts": {
    "build": "rm -rf ./dist && yarn tsc",
    "build:watch": "nodemon -e ts,js --ignore ./dist -x 'rm -rf ./dist/**/*.* && yarn tsc'"
  }
}
