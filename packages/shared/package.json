{
  "name": "@template/shared",
  "version": "1.0.0",
  "main": "dist/index.js",
  "private": true,
  "dependencies": {
    "axios": "^0.21.1",
    "typescript": "^4.1.3"
  },
  "devDependencies": {
    "@template/backend": "1.0.0"
  },
  "scripts": {
    "build": "rm -rf ./dist && yarn tsc",
    "build:watch": "mkdir -p build && nodemon -e ts,js --ignore ./dist -x 'rm -rf ./dist/**/*.* && yarn tsc'"
  }
}
